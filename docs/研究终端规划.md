# 研究终端规划与实施路线

> 产品主旨：多方法选股 → 观察队列 → 回测验证 → 复盘追踪（**不下单**）。  
> 本文是项目的产品设计、能力边界、实施路线和统一数据契约。

## 1. 项目定位

盘后 / 低频的 A 股研究推荐终端：

- 综合技术、资金、消息、博主观点，输出**可解释、可回测、可追踪的候选股票**。
- 辅助用户决策，但不自动下单，不追求实时盘口级交易，不承诺收益。

核心目标：

1. 发现候选股票
2. 给出明确风格：趋势 / 打板 / 低吸 / 龙头 / 事件 / 资金 / 观点
3. 给出推荐逻辑和证据
4. 给出关键价位和失效条件
5. 通过回测验证策略有效性
6. 通过观察队列和实际表现持续追踪

## 2. 核心用户流程

```text
打开项目
  ↓
今日推荐首页
  ↓
按风格查看候选：趋势 / 打板 / 低吸 / 龙头 / 事件 / 资金 / 观点
  ↓
点击股票，进入候选分析
  ↓
查看：为什么入选 + 证据 + 风格 + 关键价位 + 失效条件
  ↓
跳转 K 线确认
  ↓
加入观察队列
  ↓
后续复盘 / 回测 / 研究档案
```

## 3. 目标信息架构

```text
TopBar 导航
├── 今日推荐       ← 核心首页
├── 选股研究       ← 高级筛选 / 多通道
├── 观察队列
├── 回测研究
├── 研究档案
├── 数据管理
└── 行情 K 线
```

## 4. 现状盘点

### 4.1 已完成

| 模块 | 现状 |
|------|------|
| 全市场行情 | 已实现 |
| K 线 / 指标 | 已实现 |
| 自选股 | 已实现 |
| 技术选股 | 已实现，策略较多 |
| 事件 / 金十 | 已接入 |
| 观点选股 | 已实现知乎/雪球 |
| 资金面选股 | 一期已实现 |
| 龙虎榜选股 | 扶摇已接入 |
| 观察队列 | 已实现 |
| 研究工作台 | 已实现 |
| 研究档案 | 已实现 |
| 数据管理 | 已实现 |
| 技术策略回测 | 基础版已实现 |
| 参数优化 | 基础版已实现 |
| 组合回测 | 已实现 |
| 观点回测 | 已实现 |
| 今日推荐首页 | 已实现（紧凑表 + 右侧详情抽屉） |
| 统一推荐模型 | 已实现 RecommendationRecord（含结构化理由与基本面画像） |
| 基本面数据 | 已实现：估值/ROE/成长/毛利率/负债率/主力净流入并入快照 |
| 推荐表现追踪 | 已实现（含基准超额、目标/止损命中、见顶日） |
| 荐股归因 | 已实现：按维度 / 理由 / 共振数对账，输出偏差诊断 |
| 权重自校准 | 已实现 v2：风格权重 + 维度权重 + 目标价系数 + 置信度缩放 + 调整日志 |
| 模拟盘 | 已实现（次日开盘建仓、到期平仓、资金曲线） |
| 个股历史对账 | 已实现：推荐详情直接显示该股历次推荐的对错与超额 |
| 推荐准入守卫 | 已实现：冷理由 + 硬性拦截，被拦标的转入观察队列 |
| 观点历史回补 | 已实现：按时间区间回补知乎回答/文章/想法与雪球时间线，带进度、取消与幂等重跑 |
| 每日复盘摘要 | 已实现：推荐/观察/结算/归因/权重汇总，可推送（Server酱/钉钉/飞书/企微/通用 webhook） |
| 推送渠道管理 | 已实现：界面配置渠道与自动推送、发送测试、失败自动重试、发送记录 |
| 观察名单回流 | 已实现：条件改善自动回到推荐，连续被拦 ≥5 天提示理由可能长期不成立 |

### 4.2 部分完成

| 模块 | 缺口 |
|------|------|
| 回测 | 技术策略可回测，事件/资金/龙虎/风格未统一回测 |
| 推荐输出 | 各通道字段不统一，无统一推荐结论 |
| 风格识别 | 无趋势/打板/低吸/龙头等风格标签 |
| 资金面 | 无备用源，失败只能保留旧缓存 |
| 龙虎榜 | 单日榜单，无多日缓存和连续上榜 |
| 金十 | 已接入，去重/行业映射/垃圾过滤仍需优化 |
| 无人值守 | `updater` 未包含资金/龙虎/事件调度 |
| 单股 AI 点评 | 仅 Anspire，无 DeepSeek 降级 |

### 4.3 未完成

| 模块 | 说明 |
|------|------|
| 今日推荐首页 | 推荐仍散落在选股页多个 Tab |
| 统一候选模型 | 无 RecommendationRecord |
| 跨通道融合评分 | 无统一综合分 |
| 退出/失效规则 | 推荐结果未系统化展示 |
| 推荐表现追踪 | 无推荐后 N 日表现记录 |
| 风格化回测 | 打板/龙头/低吸/趋势未统一回测 |
| 通知/日报 | 无 |
| 真实交易 | 明确不做 |

## 5. 能力矩阵

| 能力 | 技术 | 事件 | 观点 | 资金 | 龙虎 | 回测 |
|---|---:|---:|---:|---:|---:|---:|
| 数据已接入 | ✅ | ✅ | ✅ | ✅ | ✅ | ⚠️ |
| 能推荐候选 | ✅ | ✅ | ✅ | ✅ | ✅ | — |
| 有统一推荐结构 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 有风格标签 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 能解释为什么选 | ✅ | ✅ | ✅ | ✅ | ✅ | — |
| 有失效条件 | ❌ | ❌ | ❌ | ❌ | ❌ | — |
| 可历史回测 | ✅ | ❌ | ⚠️ | ❌ | ❌ | — |
| 可追踪实际表现 | ❌ | ❌ | ❌ | ❌ | ❌ | — |

## 6. 功能打通关系

```text
全市场行情 + K线
        ↓
技术 / 事件 / 观点 / 资金 / 龙虎 多通道信号
        ↓
统一候选模型 RecommendationRecord
        ↓
风格识别 + 融合评分 + 风险过滤
        ↓
今日推荐首页
        ↓
候选分析 / 工作台
        ↓
观察队列
        ↓
实际表现追踪 + 历史回测
        ↓
策略权重调整
```

关键打通点：

1. 数据管理 → 推荐系统：资金/龙虎/事件刷新后推荐系统自动读取。
2. 全市场 → 选股通道：行业、涨跌幅、换手、量比、市值、PE/PB 作为统一基础过滤。
3. 观察队列 → 回测：观察队列标的成为后续表现追踪样本。
4. 回测 → 推荐排序：回测表现好的策略权重更高，反之降权。
5. 推荐 → K 线：候选分析页一键跳转完整行情。
6. 研究档案 → 复盘：人工结论与历史推荐记录关联。

## 7. 统一数据模型

### 7.1 推荐记录

```ts
interface RecommendationRecord {
  id: string
  code: string
  name: string

  // 风格
  style: 'trend' | 'limit_up' | 'pullback' | 'leader' | 'event' | 'fund' | 'opinion'

  // 来源
  channels: Array<'technical' | 'event' | 'opinion' | 'fund' | 'dragon'>

  // 结论
  thesis: string
  confidence: number
  score: number

  // 通道原始证据（简短字符串，用于快速展示）
  evidence: {
    technical?: string[]
    event?: string[]
    opinion?: string[]
    fund?: string[]
    dragon?: string[]
  }

  // 结构化推荐理由：每条都带具体数值 + 事后验证口径
  reasons: RecommendationReason[]
  reasonSummary: {
    total: number
    byDimension: Record<string, number>
    topLabels: string[]
    reasonScore: number // 理由加权强度 0~100
  }

  // 推荐当时的基本面画像
  fundamentals?: FundamentalProfile

  // 生成这条推荐时实际使用的权重（可追溯、可对账）
  appliedWeights: {
    style: number
    dimension: number
    targetFactor: number
    confidenceScale: number
  }

  // 价位与失效
  levels: { entry?: number; target?: number; stopLoss?: number }
  invalidIf: string[]
  horizonDays: number

  // 时间与基准
  signalDate: string
  createdAt: number
  benchmark: string // 默认 sh000001，用于计算超额收益
}

interface RecommendationReason {
  dimension: 'fundamental' | 'technical' | 'fund' | 'dragon' | 'event' | 'opinion' | 'industry'
  key: string
  label: string   // 例：估值 低估 / 业绩高增长 / 主力连续净流入 / 博主看多
  detail: string  // 例：PE(TTM) 11.4，行业中位 24.4，行业估值分位 23%
  weight: number  // 该理由在结论中的权重
  strength: number // 理由强度 0~100
  metrics: Record<string, number | string> // 机器可读数值
  expect: string  // 事后验证口径，归因时按同一口径判定成立/证伪
  sources?: ReasonSourceLink[]
}

interface FundamentalProfile {
  peTtm?: number; peStatic?: number; pb?: number
  roe?: number
  revenueYi?: number; revenueYoy?: number
  netProfitYi?: number; profitYoy?: number
  grossMargin?: number; netMargin?: number; debtRatio?: number
  mainNetInflowYi?: number; mainNetInflowPct?: number
  mktcapYi?: number
  industryPeMedian?: number; industryPePercentile?: number
  valuationLabel?: string; growthLabel?: string; qualityLabel?: string
  rating: 'strong' | 'neutral' | 'weak'
  bullets: string[]
}
```

> 基本面来源：东财 clist 追加字段（f37 ROE / f41 营收同比 / f46 净利同比 / f49 毛利率 / f57 负债率 / f62 主力净流入 / f115 PE(TTM) / f100 行业 / f103 概念），
> 由 `refreshSlowVars` 合并进行情快照；东财不可达时主机自动降级（push2delay → push2 → 82.push2），字段缺失时理由只输出可得部分，不编造数值。

### 7.2 回测记录

```ts
interface BacktestRecord {
  strategyId: string
  style: string
  startDate: string
  endDate: string
  metrics: {
    trades: number
    winRate: number
    avgReturn: number
    maxDrawdown: number
    profitFactor: number
    excessReturn: number
  }
  passed: boolean
}
```

### 7.3 推荐表现追踪

```ts
interface RecommendationOutcome {
  recommendationId: string
  signalDate: string
  entryPrice?: number
  maxGainPct?: number
  maxLossPct?: number
  endPrice?: number
  returnPct?: number
  hitTarget: boolean
  hitStop: boolean
  invalidated: boolean
}
```

### 7.4 荐股归因闭环（理由 → 对账 → 改策略）

这是「推荐质量」的主链路，缺了它，推荐就只是每天一屏列表：

```
通道 / 基本面 / 资金 / 观点  ─┐
                            ├─→ RecommendationReason（带具体数值 + 验证口径）
                            │
推荐记录落库（含理由、权重快照）│
                            ↓
持有期结束 → RecommendationOutcome ─→ 逐条对账（涨跌 / 超额 / 目标 / 止损 / 见顶日）
                            ↓
              RecommendationAttribution（按维度、按理由、按共振数、校准、偏差）
                            ↓
        RecommendationWeights v2（风格权重 / 维度权重 / 目标价系数 / 置信度缩放 + 调整日志）
                            ↓
                     回写到下一次推荐（appliedWeights 可追溯）
```

关键口径：

| 概念 | 定义 |
|------|------|
| 对账结果 | 信号日次日开盘买入，持有 `horizonDays` 个交易日后收盘卖出（与模拟盘一致） |
| 基准 | 上证指数（`sh000001`），按实际买卖日对齐，停牌不影响 |
| 维度成立 | 该条推荐的该维度理由出现时，实际收益是否跑赢基准（excessPct > 0） |
| 校准误差 | 置信度区间内的预测胜率与实际胜率的加权偏差（ECE） |
| 目标高估 | 目标价隐含涨幅 − 实际平均收益 |
| 见顶日 | 持有期内最高点距买入的交易日数，用于判断持有期是否合理 |

输出（`GET /api/recommendations/attribution`）：

- `byDimension`：每个理由维度的样本数、上涨率、跑赢率、平均收益、平均超额、结论（有效 / 一般 / 无效 / 样本不足）
- `byLabel`：具体理由（估值低估、业绩高增长、主力连续净流入、博主看多…）的跑赢率
- `resonance`：按理由维度数分桶（1 / 2 / 3+），验证「多维共振是否更可靠」
- `bias`：目标价隐含 vs 实际、目标命中率、止损触发率、平均见顶日、置信度校准表
- `suggestions`：可直接执行的结论，例如「资金面理由 12 个样本跑赢大盘 33% → 权重下调至 0.72」

### 7.5 权重自校准（v2）

`data/recommendation-weights.json`（v2）：

```ts
{
  version: 2,
  weights: { trend: 1.08, limit_up: 0.9, ... },        // 风格权重
  dimensionWeights: { fund: 1.15, opinion: 0.72, ... }, // 理由维度权重
  targetFactor: 0.62,        // 目标价系数（按实际可达幅度缩放）
  confidenceScale: 0.86,     // 置信度整体缩放（按校准误差）
  adjustments: [{ at, scope, key, from, to, samples, reason }] // 每次调整的依据
}
```

- 仅当某维度样本 ≥ 5 时才调整，且新旧权重按 0.5 平滑，避免小样本过拟合。
- 置信度公式：`evidenceScore × 验证系数 × 风格权重 × 维度权重 × confidenceScale`，
  其中 `evidenceScore = 通道分 × 0.6 + 理由分 × 0.4`。
- 目标价 = 入场价 × (1 + 基础目标幅度 × targetFactor)，避免系统性高估。
- 每次生成推荐都会写入 `appliedWeights`，事后可回答「这条推荐当时是按什么标准选出来的」。

## 8. 回测体系

回测是推荐系统的验证器，不是附属功能。

### 8.1 按风格定义回测规则

| 风格 | 回测思路 |
|------|----------|
| 趋势 | 均线/突破后持有 N 日，移动止损 |
| 低吸 | 超跌/回踩后买入，观察反弹 |
| 龙头 | 行业相对强度 + 换手 + 涨幅，持有 N 日 |
| 打板 | 涨停次日表现；必须考虑涨停不可买入 |
| 事件 | 事件发生后 N 日窗口收益 |
| 资金 | 主力连续净流入后 N 日表现 |
| 观点 | 博主共识后 N 日表现，样本稀疏时降权 |

### 8.2 统一评价指标

- 胜率
- 平均收益
- 中位收益
- 盈亏比
- 最大回撤
- 相对沪深300 / 中证500 / 同行业超额收益
- 目标命中率
- 止损触发率
- 交易次数是否足够

### 8.3 防过拟合

- 样本内 / 样本外分开
- 走步式回测
- 参数搜索范围限制
- 交易次数过少时标记“不可信”

### 8.4 成交可行性与偏差

- 涨跌停无法成交
- 一字板无法买入
- 停牌 / 流动性不足
- 避免未来函数
- 避免幸存者偏差

## 9. 分阶段实施计划

### P0：统一“推荐”这件事（当前优先）

1. 定义统一 RecommendationRecord。
2. 各通道输出统一结构。
3. 增加风格标签规则。
4. 开发「今日推荐」首页。
5. 推荐结果落库。

### P1：建立回测闭环（与 P0 同等重要）

1. 搭建统一回测引擎。
2. 支持技术、资金、龙虎、事件通道回测。
3. 增加风格回测：趋势 / 低吸 / 龙头 / 打板。
4. 统一回测指标。
5. 增加回测看板。

### P2：数据补强与推荐质量

1. 资金流备用源。
2. 龙虎榜多日缓存。
3. 基本面 B1：估值条件 + 轻量估值通道。
4. 消息面去重和行业映射优化。
5. 推荐冷却 / 去重 / 同板块集中度控制。

### P3：追踪与自动优化

1. ~~推荐表现追踪~~ 已完成（`/api/recommendations/performance`）。
2. ~~策略自动降权 / 升权~~ 已完成（`/api/recommendations/weights` v2 + 归因驱动校准）。
3. ~~荐股理由结构化与事后对账~~ 已完成（`/api/recommendations/attribution`）。
4. ~~每日推荐摘要通知~~ 已完成（`/api/digest` + 每日复盘页 + webhook 推送，支持收盘后自动生成）。
5. 市场环境过滤：大盘趋势、情绪、涨跌停家数（温度已用于权重与复盘首段）。
6. 更深的基本面数据：财报明细、行业景气度、机构一致预期。
7. ~~归因结果反哺通道~~ 已完成：冷理由（跑赢率 < 40% 且样本 ≥ 8）与硬性拦截（ST、资产负债率 ≥ 80%、主力大幅净流出）自动转观察名单。
8. ~~复盘推送渠道管理~~ 已完成：渠道界面配置（打码存储）、发送测试、失败自动重试（可配次数/间隔）、发送记录与手动重试。
9. ~~按时间回补历史观点~~ 已完成：`GET/POST /api/opinions/backfill`（sinceDate + kinds + cancel），逐个博主逐页翻到日期边界，
   状态落盘 `data/opinion-backfill.json`，LLM 抽取并发 3，已入库内容不重复分析；用于给观点回测补足博主样本量。

## 10. 风险、边界与补充

- 通道产出的是候选，不是买卖信号。
- 密钥只放服务端 .env，不进仓库、不回显前端。
- 优先可解释、可缓存、可调度的规则聚合；LLM 仅用于解析/抽取。
- 打板回测必须考虑成交可行性。
- 同一板块、同一逻辑的股票避免过度集中推荐。
- 大盘明显弱势时，趋势和打板策略应自动降权。
- 记录数据来源、更新时间、失败原因、是否使用缓存。
- 支持人工复盘：用户是否采纳、结果如何。

## 11. 数据源能力对照

| 需求 | 当前方案 | 备注 |
|------|----------|------|
| 主力净流入序列 | 东财 | 网络不稳时需降级或换源 |
| 龙虎榜 | 扶摇 | 需 FUYAO_API_KEY |
| 个股资金流 API（扶摇） | ❌ | MCP 无权限；勿与现金流量表混淆 |
| 快讯/日历 | 金十 MCP | JIN10_MCP_TOKEN |
| 报价/K 线 | 腾讯/东财/TickFlow | — |

## 12. 环境变量（服务端）

| 变量 | 用途 |
|------|------|
| JIN10_MCP_TOKEN / JIN10_MCP_URL | 金十快讯与日历 |
| FUYAO_API_KEY / FUYAO_API_BASE | 扶摇龙虎榜 |
| ZHIHU_COOKIE / ZHIHU_ACCESS_SECRET | 知乎观点同步 |
| XUEQIU_COOKIE | 雪球观点同步 |
| DEEPSEEK_API_KEY / ANSPIRE_* / TICKFLOW_API_KEY | 既有 AI / 行情 |

详见仓库根目录 .env.example。

## 13. 推荐推进顺序

1. P0：统一推荐结构 + 今日推荐首页
2. P1：统一回测框架 + 风格化回测 + 回测看板
3. P2：资金备用源 + 龙虎多日 + 基本面 B1
4. P3：推荐追踪 + 策略调权 + 通知

---

## 14. 已完成模块与暂存方案（Parking Lot）

> 本节用于「一个板块做到可用就停手」的节奏管理：已完成项归档，未做项明确写出触发条件，避免遗忘或重复讨论。
> 当前两个「已做到可用、暂时封板」的板块：**观点研究（14.1）**、**涨停板与情绪周期（14.4）**；其余零散项见 14.2。
> 恢复开发时的建议顺序见 14.5。

### 14.1 观点研究（已完成主体，暂存以下项）

| 状态 | 项 | 触发条件 / 备注 |
|------|----|----------------|
| 已完成 | 知乎回答/文章/想法采集、雪球时间线、增量同步、按时间回补、结构化抽取、观点共识、观点回测、博主可靠性 | 见 7.x 与 `/api/opinions/*` |
| 已完成 | 回补结论：**想法在 3/5/10/20 日全部负超额，长文（回答+文章）稳定正超额** | 分层回测口径见 14.3 |
| 已完成 | 观点通道分层：想法权重 0.4、长文 1.0、手工 0.8；仅想法支撑的信号理由权重 0.16 | `OPINION_KIND_WEIGHT`、`opinion-signals` |
| 已完成 | 博主可靠性接入推荐权重：作者维度贝叶斯命中率落盘，opinion 通道按博主加权 | `GET/POST /api/opinions/author-stats` |
| 已完成 | `evidenceQuote` 逐字校验：落库即打标，`verifiedOnly` 可过滤；存量补打接口 `/api/opinions/verify-quotes` | 3593 条 claims 命中 3452 条（96.1%） |
| 暂存 | 雪球历史回补（4 个订阅） | **需先更新 `XUEQIU_COOKIE`**，当前返回 400 |
| 暂存 | 想法分时级信号（发布时间与盘中价格） | 需要分时数据与更细的回测口径 |

### 14.2 其它板块暂存项

| 板块 | 暂存项 |
|------|--------|
| 通知 | 推送渠道多目标、失败告警、日报模板可配置 |
| 数据 | 行情快照历史留档检索、财务季度数据、行业景气度、机构一致预期 |
| 回测 | 参数优化接入推荐权重、样本外滚动验证、组合层面风险预算 |
| AI | 个股点评结果缓存与版本管理、多模型对比 |
| 运维 | 具名 Cloudflare 隧道（稳定公网地址）、日志轮转 |

### 14.3 观点回测口径（回补后基线，2026-09-15）

```text
口径：信号日次日开盘买入，持有 N 个交易日收盘卖出，基准沪深300；claims 需「有 A 股代码 + 有方向」

队列                      claims  已评估  命中率   平均方向收益   平均超额
想法 pin                   151     132    37.9%      -4.53%      -3.39%
长文 回答+文章              188     139    48.9%      +3.73%      +3.81%
手工导入(雪球)               88      39    51.3%      +4.72%      +5.43%
全量（回补前基线）           279     178    49.4%      +3.95%      +4.17%
全量（回补后）               427     310    44.5%      +0.34%      +0.95%

持有期敏感性：想法 3/5/10/20 日全部负超额；长文 5 日 +1.17%、10 日 +2.43%、20 日 +3.81%
```

结论：**内容类型决定下限，选对博主决定上限**。想法类内容不适合作为荐股依据，但可作为情绪与热度参考。

**逐字引用校验后的复核（2026-09-16）**：全部 3593 条 claims 中 3452 条引用能在原文逐字命中（96.1%）；
仅保留逐字命中样本后结论不变——想法 -3.41%、长文 +3.15%（原 -3.50% / +3.39%），说明结论不是抽取幻觉造成的。

**博主可靠性（20 日持有期，2026-09-16 刷新）**：

```text
Dr.Bin       n=100  命中 53.0%  可靠性 0.585  平均超额 +4.70%
卧着吹牛      n= 30  命中 36.7%  可靠性 0.400  平均超额 +2.06%
向阳         n= 61  命中 37.7%  可靠性 0.352  平均超额 -1.73%
萧铭meaning  n=106  命中 34.9%  可靠性 0.312  平均超额 -3.66%
```

### 14.4 涨停板与情绪周期（已完成主体，暂存以下项）

| 状态 | 项 | 触发条件 / 备注 |
|------|----|----------------|
| 已完成 | 精确涨跌停判定（主板 10% / 科创·创业 20% / 北交所 30% / ST 5%）、连板高度回溯、分时首次封板时间与炸板次数、板块梯队（申万行业 + 东财概念）、辨识度评分、情绪五相 | 见 §15 与 `server/limit-up.ts` |
| 已完成 | 推荐侧 `board` 通道：龙头 `leader` / 打板 `limit_up` / 补涨 `relay`，情绪相位驱动风格权重，冰点·退潮期高位板进观察名单 | `PHASE_STYLE_MULTIPLIER`、`detectVetoes` |
| 已完成 | 修正基础事实：旧口径 `changePct>=9.8` 会把 8 只创业板·科创板 12~16% 个股误判为涨停 | 2026-09-15 实测 |
| 已完成 | **涨停池历史回测**：本地日K重算 2012-12 至今（1440 个交易日），打板 / 接力双口径 + 可成交口径剔除一字板 | `GET/POST /api/limit-up/backtest`、涨停板页「历史回测」 |
| 已完成 | 情绪相位与板位权重校准：退潮压低打板/龙头、冰点抬升低位首板、补涨整体降权、5 连板以上进观察名单 | 依据见 15.6 |
| 已完成 | 三条资金线合并打分：涨停辨识度 × 龙虎榜席位 × 主力资金 → 资金共识分（含广度加成） | `server/capital-consensus.ts` |
| 待观察 | `board` 维度归因：等该维度累积 5~10 个成熟样本后，用真实跑赢率自动调权 | 触发条件：归因页 `board` 维度样本 ≥ 5（约 1~2 周） |
| 暂存 | 「昨日涨停今日溢价 / 晋级率」接入每日复盘摘要首段（现在只在涨停板页与推荐理由里） | 与日报模板改造一起做 |
| 暂存 | 分时覆盖扩大：当前只对成交额前 40 只涨停股取分时算封板时间，可放开到全部涨停股或按需逐只补 | 成本：每只 1 次分时请求 |
| 暂存 | 一字板 / 秒板单独标记（09:30 即封板），作为强度最高的一档（辨识度加分更陡） | 分时数据已具备，仅需补判定分支 |
| 暂存 | 晋级率按板位拆分（2 板晋级率、3 板晋级率、最高板晋级率），比单一整体晋级率更能刻画空间高度 | 需要归档保留昨日分板位名单（已存 board 字段，可直接算） |
| 暂存 | 可成交性的进一步建模：封单量、竞价强度、板块内排队顺序（现在只用「一字板」粗过滤） | 需要 Level1/竞价数据 |
| 暂存 | 晋级率按板位拆分的历史序列（2 板晋级率、最高板晋级率） | 历史池已按板位存好，可直接统计 |
| 暂存 | 涨停原因 / 题材归因数据源：拿到「涨停原因」可把板块梯队做成可读的题材线索 | 需新增数据源（金十/东财涨停原因接口） |
| 暂存 | 封单额与封成比：现在只有成交额与封板时间，缺封单金额（Level1/龙虎榜口径） | 需数据源支持 |
| 暂存 | 与龙虎榜联动：涨停 + 知名游资/机构席位 = 更高辨识度（现在两条通道相互独立） | 数据已在 `dragon-tiger-rank.json`，缺合并打分 |
| 暂存 | 新股/退市整理期口径细化（上市前 5 日无涨跌幅限制、退市整理期规则不同） | 当前仅靠 K 线长度与名称粗过滤 |

### 14.5 恢复开发时的建议顺序

1. **涨停板先闭环**（数据已具备，只差时间积累）
   - 等 `board` 维度样本达 5~10 个 → 看归因页的跑赢率，让权重自校准
   - 同时用 `limit-up-history.json`（≥20 个交易日）验证情绪相位阈值，校准冰点/退潮判定
2. **观点通道分层**（结论已明确，改动集中在推荐侧）
   - 想法降权 / 仅作情绪参考；博主可靠性接进 opinion 权重；补 `evidenceQuote` 逐字校验复核结论
3. **三条资金线合并打分**（跨板块，收益最直接）
   - 涨停板（辨识度/板位） + 龙虎榜（游资/机构席位） + 主力资金，合成一个「资金共识分」，替换现在相互独立的通道打分
4. **零散补强**：涨停原因数据源、封单额、日报首段接入情绪指标、分时覆盖扩大

## 15. 涨停板与情绪周期模块（P4，已实现）

> 实现状态：`server/limit-up.ts` + `/api/limit-up` + 「涨停板」页面 + 推荐侧 `board` 通道（leader / limit_up / relay）。
> 实测（2026-09-15）：精确口径下涨停 36 / 跌停 48 / 炸板 31，旧口径 `changePct>=9.8` 会把 8 只创业板·科创板 12~16% 的个股误判为涨停。

### 15.1 为什么值得单独做

板块启动几乎总是由**某一支个股的涨停**打出辨识度：先有龙头封板，再有同板块跟随，最后出现分歧与退潮。
因此「涨停 → 连板 → 板块梯队 → 情绪相位」是推荐系统里最贴近 A 股真实交易结构的一条线，
也是现有实现最薄弱的环节（当前只用 `changePct >= 9.8` 近似判定涨停，创业板/科创板 20% 涨跌幅会被误判）。

### 15.2 数据口径（先修正基础事实）

| 项 | 口径 |
|----|------|
| 涨跌幅限制 | 主板 10%、科创板 688 / 创业板 300·301 20%、北交所 30%、ST 主板 5%（ST 创业板/科创板仍 20%） |
| 涨停判定 | `price >= round(prevClose × (1 + 限制), 2)`，**不再用 9.8% 近似** |
| 跌停判定 | 同理向下取整 |
| 炸板 | `high` 触及涨停价但收盘未封住；并记录炸板次数（分时） |
| 连板高度 | 用日 K 逐日回溯：`close(d) == 涨停价(close(d-1))` 的最大连续天数 |
| 首次封板时间 | 用当日 1 分钟 K 线，第一根 `close == 涨停价` 的时间（越早越强） |
| 板块 | 申万一级行业 + 东财概念（快照 `concepts`）双维度聚合 |

### 15.3 输出的指标体系

```ts
interface LimitUpBoard {
  date: string
  limitUp: BoardItem[]      // 涨停池：板数 / 首次封板时间 / 成交额 / 换手 / 主力净流入 / 板块
  limitDown: BoardItem[]    // 跌停池
  broken: BoardItem[]       // 炸板池（含炸板次数）
  ladder: Record<number, number>  // 连板梯队：1板 N 家、2板 N 家……
  sectors: SectorBoard[]    // 板块梯队：涨停家数 / 最高板 / 龙头 / 平均涨幅
  sentiment: {
    limitUpCount, limitDownCount, brokenRate, maxBoard,
    promotionRate,          // 昨日首板→今日连板的晋级率
    yesterdayPremium,       // 昨日涨停股今日平均涨幅（打板赚钱效应）
    phase: '冰点' | '启动' | '发酵' | '高潮' | '退潮'
    score: number
  }
}
```

**辨识度评分**（每只涨停股）：连板高度 40 + 板块内涨停家数 20 + 成交额排名 20 + 首次封板时间 15 + 炸板扣分 5。
用户提到的「打出辨识度」在这里落地为：**同一板块内，辨识度最高 = 板块龙头**。

### 15.4 情绪相位 → 推荐权重（把温度计从二元升级为五相）

| 相位 | 判定（简化） | 推荐取向 |
|------|-------------|---------|
| 冰点 | 涨停 < 30 且最高板 ≤ 2 | 首板/低吸优先，禁高位接力 |
| 启动 | 涨停 30–60，炸板率 < 30% | 首板 + 板块内补涨（性价比最好） |
| 发酵 | 涨停 60–100，最高板 3–5 | 龙头接力 + 补涨 |
| 高潮 | 涨停 > 100 或最高板 ≥ 5 | 只做龙头，警惕分歧 |
| 退潮 | 炸板率 > 45% 或跌停 > 30 或昨日溢价为负 | 打板/龙头权重下调，低吸/趋势优先 |

### 15.5 落到推荐（本模块的唯一目的）

新增理由维度 `board` 与三个风格：

1. **`leader` 龙头**：板块内辨识度第一且连板 ≥ 2 → 接力逻辑，验证口径「次日不炸板、3 日内不破 5 日线」
2. **`limit_up` 打板**：板块内涨停家数 ≥ 3 的首板（低位、有板块效应）→ 验证口径「次日高开或继续封板」
3. **`relay` 补涨**：热点板块内尚未涨停、但量价与资金配合的二线 → 验证口径「3 日内跑赢板块」

配套约束：
- 退潮/冰点相位下高位板直接进入观察名单（复用 `recommendation-guard`）
- 炸板股、跌停股不进入推荐池
- 每条推荐都带板块上下文理由（「CCL 概念 5 家涨停、最高 3 板，本股首板 09:41 封板、成交额板块第 2」）

### 15.6 历史回测结论（2012-12 ~ 2026-09，1440 个交易日 / 5411 只）

```text
口径：信号日=涨停当天；打板=涨停日收盘买、次日收盘卖；接力=次日开盘买、次日收盘卖
可成交口径 = 剔除当日一字板（买不到）与次日一字板，共剔除 4373 个样本

                 打板胜率   打板均值   打板持有3日   接力均值
全样本            58.2%     +1.94%     +2.08%     -0.35%
可成交口径         55.1%     +1.36%     +0.99%     -0.30%
  1板             55.2%     +1.40%     +1.14%     -0.25%
  2板             53.2%     +1.14%     +0.35%     -0.52%
  3板             56.5%     +1.42%     +0.78%     -0.42%
  4板             57.0%     +1.09%     -0.20%     -0.52%
  5板及以上         55.2%     +1.00%     +0.04%     -0.70%

可成交 · 按相位：冰点 +1.32% / 启动 +0.89% / 发酵 +1.14% / 高潮 +1.83% / 退潮 +0.52%（3 日 -0.28%）
```

**三条结论（已写进权重与守卫）**：

1. **打板赚的是隔夜溢价，不是盘中接力**：打板口径全线正期望，接力口径全线负期望，且板位越高越差。
2. **剔除一字板后「板位越高越好」消失**（全样本 5 板 +3.36% → 可成交 5 板 +1.00%）：高板位的超额主要来自买不到的一字板。
   因此高位板不再额外加权，5 连板以上直接进观察名单。
3. **退潮期打板收益最低**（+0.52%，3 日转负），冰点期反而最好（+1.32%）→ 相位权重据此调整。

### 15.7 验证与防过拟合

- 复用统一归因：`board` 维度的跑赢率与平均超额独立统计，与其它维度可比
- 连板高度、首次封板时间等指标进入结构化理由，**回测时按同一口径判定成立/证伪**
- 情绪相位的历史序列落盘（`data/limit-up-history.json`），可用于「相位 vs 次日打板溢价」的统计验证
- 注意样本偏差：注册制 20% 涨跌幅、北交所 30% 与主板 10% 不可混算，回测需按涨跌幅限制分组

---

*文档随实现更新；若与代码不一致，以代码与 API 行为为准。*
